import type { PrismaClient } from "@prisma/client";

// Demo catalog data removed. Products, tasks, and announcements are managed
// through the admin UI at /admin/digital-products, /admin/get-paid-tasks,
// and /admin/announcements.
export async function seedAffsenseCatalog(_prisma: PrismaClient) {
  // intentionally empty
}
